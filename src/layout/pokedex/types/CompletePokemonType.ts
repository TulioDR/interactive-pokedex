export interface CompletePokemonType {
   base: any; // Holds the complete raw object from /pokemon/[id]
   species: any; // Holds the complete raw object from /pokemon-species/[id]
   evolution: any;
}
